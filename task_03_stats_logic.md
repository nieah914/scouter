# Task: Health Stats & Percentile Calculation Logic
이제 백엔드 계산의 핵심인 `app/services/stats.py` 파일을 작성할 거야.
유저가 입력한 원시 데이터(키, 몸무게, 푸시업/러닝 기록 등)를 바탕으로, 대한비만학회 기준 BMI와 '국민체력100' 데이터 기반 백분위를 산출하는 로직을 구현해 줘.

## 1. BMI 산출 로직 (`calculate_bmi_status`)
- 공식: `BMI = 체중(kg) / (키(m) * 키(m))`
- 대한비만학회 2025 최신 기준을 적용하여 상태를 반환해 줘.
  - 18.5 미만: 저체중 (underweight)
  - 18.5 ~ 22.9: 정상 (normal)
  - 23.0 ~ 24.9: 과체중/비만전단계 (overweight)
  - 25.0 ~ 29.9: 1단계 비만병 (obese_1)
  - 30.0 ~ 34.9: 2단계 비만병 (obese_2)
  - 35.0 이상: 3단계 고도비만 (obese_3)

## 2. 근력(푸시업) 및 심폐(러닝) 상위 % 산출 로직 (`calculate_percentile`)
- **선형 보간법(Linear Interpolation)**을 사용하여 유저의 기록이 상위 몇 %인지 계산하는 함수를 만들어 줘.
- 아래의 기준 테이블(Dictionary)을 하드코딩해서 사용해 줘. (1분 푸시업 개수 기준, 남성 예시)
  ```python
  # 나이대별 [상위 10% 컷, 상위 30% 컷, 상위 50% 컷, 상위 80% 컷]
  pushup_table_m = {
      20:,[1][2][3][4]
      30:,[5][6][7][8]
      40:[4][9][10][11]
  }
계산 룰:

점수가 상위 10% 컷 이상이면: "상위 10% 이내 (S급)" 반환

상위 30% 컷 ~ 상위 10% 컷 사이면: 10%~30% 사이에서 비율 계산 (예: 상위 23%)

상위 50% 컷 ~ 상위 30% 컷 사이면: 30%~50% 사이에서 비율 계산

상위 80% 컷 미만이면: "하위 50% 미만" 반환

(러닝 데이터도 위와 동일한 구조의 더미 테이블 running_table_m을 임의로 만들어서 함수를 범용적으로 쓸 수 있게 짜줘.)

## 3. 스탯 점수 환산 로직 (convert_raw_to_scores)
다음 단계(캐릭터 분류)를 위해 유저의 입력을 0~100점 스케일로 환산하는 헬퍼 함수를 만들어 줘.

str_score: 푸시업 상위 %를 점수화 (상위 10%면 90점, 하위면 30점 등 임의 매핑)

agi_score: 러닝 상위 %를 점수화

hp_score (수면 시간):

7시간 이상 = 100점, 6시간 = 70점, 5시간 = 40점, 4시간 이하 = 10점

debuff_score (독/저주):

공식 = (하루 커피 잔 수 * 10) + (주당 야근 횟수 * 15)

최대 100점을 넘지 않도록 min(score, 100) 처리.

## 4. Action Required
위의 요구사항을 모두 충족하는 깨끗하고 타입 힌트(Type Hinting)가 적용된 app/services/stats.py 코드를 작성해 줘.

---

## ✅ 변경 사항 (Q7~Q10 반영)

### `convert_raw_to_scores()` 함수 파라미터 확장
기존 7개 파라미터 → 11개로 확장 (신규 4개 모두 default값 있음):

| 파라미터 | 타입 | 기본값 | 의미 |
|---------|------|--------|------|
| `diet_score` | int | 0 | Q7 식습관: 0=건강식, 1=보통, 2=패스트푸드, 3=인스턴트 |
| `stress_score` | int | 0 | Q8 스트레스: 0=없음, 1=낮음, 2=높음, 3=극한 |
| `sitting_hours` | int | 6 | Q9 좌식 시간: 4/6/10/13 (일 평균) |
| `alcohol_freq` | int | 0 | Q10 음주 일수: 0/1/3/7 (주간) |

### 스탯 계산 로직 변경

**AGI 패널티 (Q9 좌식 시간)**
```python
sit_penalty = {4: 0, 6: 5, 10: 15, 13: 25}.get(sitting_hours, 10)
agi_score = max(5, agi_score - sit_penalty)
```

**HP 패널티 (Q7 식습관)**
```python
diet_hp_penalty = [0, 10, 20, 30][min(diet_score, 3)]
hp_score = max(5, base_hp - diet_hp_penalty)
```

**DEBUFF 계산식 확장 (기존 공식에 신규 항목 추가)**
```python
diet_debuff   = [0, 0, 5, 15][min(diet_score, 3)]
stress_debuff = {0: 0, 1: 10, 2: 25, 3: 40}.get(stress_score, 0)
alc_debuff    = {0: 0, 1: 5, 3: 15, 7: 30}.get(alcohol_freq, 0)
debuff_score  = min(100,
    (coffee_cups * 10) + (overtime_days * 15) +
    diet_debuff + stress_debuff + alc_debuff
)
```

### Q5 심폐 측정 방식 변경 (프론트 only, 백엔드 불변)
셔틀런 횟수를 직접 묻는 방식이 일반인에게 생소하여 **일상 달리기 경험 기반 자가진단**으로 전환.
내부적으로는 동일한 `SHUTTLERUN_TABLE` 기반 백분위 계산을 그대로 사용하며,
선택지 value(65/50/35/20/0)는 기존 셔틀런 횟수에 매핑된 그대로 유지됨.

| value | 내부 매핑 | 프론트 표시 |
|-------|-----------|------------|
| 65 | 셔틀런 60회+ / 상위 10% | 뛰면서 전화통화도 거뜬 / 1km 5분대 |
| 50 | 셔틀런 40~60회 / 상위 30% | 1km 쉬지 않고 달림 / 6~7분대 |
| 35 | 셔틀런 25~40회 / 중위 | 달리다 중간에 걷게 됨 / 8~10분대 |
| 20 | 셔틀런 25회 미만 / 하위 | 100m만 뛰어도 숨참 |
| 0 | 운동 거의 안 함 | 버스 놓칠 때만 달림 |
